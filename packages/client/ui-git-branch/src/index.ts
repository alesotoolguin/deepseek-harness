/** Host loader entry for the session-header branch badge browser implementation exported from `./client`. */

/** Host plugin body — branch resolution lives in the @deepseek-ai/dsh-host-git-branch row. */
export function apply(): void {}
